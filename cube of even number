#include<Stdio.h>
int main(){
    int num, cube;
    printf("Enter number ");
    scanf("%d", &num);
    if(num%2 == 0)
    {
         cube = num * num * num;
        printf("Cube of %d is %d", num, cube);

    }
    return 0;
}
